//here we will render the dance floor cells and export them

import React from 'react';
import './index.scss';

const nameFunction = (name) => {
  console.log('raquel')
}

export default nameFunction
/*
const Dancefloor = ({ createCells }) => {
  return createCells.map(i => <div className="cell" ></div> )
}

export default Dancefloor
*/

